# pecl dependencies
# =================
# package = pecl package name
# version = general or specific package version
# libname = name of the shared object if it differs from the package name
# ---------------------------------------------------------------------------
#
# package,	version,	libname

raphf,		1.0.4
propro,		stable
pecl_http,	2.0.3,		http.so
markdown,	stable,		discount.so
